name: pose_detection
description: "Pose detector"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  file_picker: ^9.2.1
  flutter:
    sdk: flutter
  google_ml_kit: ^0.20.0
  image_picker: ^1.1.2
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
